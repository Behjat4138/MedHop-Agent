# Reranker + eval harness dependencies (Person B)
# Python 3.10+
# Install from repo root:  pip install -r reranker/requirements.txt

# Cross-encoder model + training utilities
sentence-transformers==3.0.1

# Transformers (pin matches Person A's retriever/requirements.txt)
transformers==4.41.2
torch==2.3.0

# Numerics / utilities (pin matches Person A for consistency)
numpy==1.26.4
tqdm==4.66.4